<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>โครงการบริการวิชาการ และ CPE</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400;1,700&display=swap" rel="stylesheet">
<style>

/* ═══════════════════════════════════════════════
   AURA core reset + animations
═══════════════════════════════════════════════ */
*{box-sizing:border-box;margin:0;padding:0}
body{
  font-family:'Kanit',sans-serif;
  min-height:100vh;background:#eceff5;
  color:#16181f;overflow-x:hidden;position:relative;
}

@keyframes auFloat{0%,100%{transform:translateY(0)}50%{transform:translateY(-20px)}}
@keyframes auFloatB{0%,100%{transform:translateY(0)}50%{transform:translateY(14px)}}
@keyframes auSpin{to{transform:rotate(360deg)}}
@keyframes auRise{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
@keyframes auShimmer{to{background-position:220% center}}
@keyframes rpl{to{transform:scale(2.6);opacity:0}}

/* ═══════════════════════════════════════════════
   REACTIVE BG ORBS  (same as AURA standalone)
═══════════════════════════════════════════════ */
.au-orb{position:fixed;border-radius:50%;pointer-events:none;z-index:1}
.au-orb1{
  width:560px;height:560px;
  background:conic-gradient(from 0deg,#86c5ff,#c5a0ff,#ff95d0,#95f5d0,#fff0a0,#86c5ff);
  filter:blur(82px) hue-rotate(calc(var(--hue,0) * 1deg));opacity:.62;
  left:calc(10% + var(--mx,.5) * 52%);top:calc(-24% + var(--my,.5) * 54%);
  animation:auSpin 26s linear infinite;
}
.au-orb2{
  width:420px;height:420px;
  background:conic-gradient(from 130deg,#95f5d0,#86c5ff,#c5a0ff,#95f5d0);
  filter:blur(72px) hue-rotate(calc(var(--hue,0) * -0.8deg));opacity:.52;
  left:calc(52% - var(--mx,.5) * 52%);top:calc(30% + var(--my,.5) * 40%);
  animation:auSpin 38s linear infinite reverse;
}
.au-orb3{
  width:290px;height:290px;
  background:conic-gradient(from 240deg,#ffd090,#ff95d0,#b9a4ff);
  filter:blur(60px) hue-rotate(calc(var(--hue,0) * 1.6deg));opacity:.48;
  left:calc(4% + var(--mx,.5) * 24%);top:calc(14% + var(--my,.5) * 50%);
  animation:auSpin 44s linear infinite;
}

/* cursor spotlight */
#au-cursor{
  position:fixed;width:520px;height:520px;border-radius:50%;
  background:radial-gradient(circle at center,rgba(255,255,255,.22),transparent 65%);
  pointer-events:none;z-index:5;transform:translate(-50%,-50%);
  transition:left .06s,top .06s;
}

/* ═══════════════════════════════════════════════
   NAV GLASS
═══════════════════════════════════════════════ */
nav{
  display:flex;align-items:center;justify-content:space-between;
  padding:20px 52px;position:sticky;top:0;z-index:20;
  background:rgba(245,247,252,.44);
  backdrop-filter:blur(24px);-webkit-backdrop-filter:blur(24px);
  border-bottom:1px solid rgba(200,210,230,.5);
}
.nav-brand{display:flex;align-items:center;gap:11px}
/* AURA spinning dot */
.au-dot{
  width:13px;height:13px;border-radius:50%;flex-shrink:0;
  background:conic-gradient(from 0deg,#7cc6ff,#c9a6ff,#ff9ecf,#8ef0c4,#7cc6ff);
  animation:auSpin 8s linear infinite;
  filter:hue-rotate(calc(var(--hue,0)*1deg));
}
.brand-name{font-family:'Kanit',sans-serif;font-weight:700;font-size:18px;letter-spacing:.04em;color:#16181f}
.nav-mid{display:flex;align-items:center;gap:8px;font-family:'Kanit',sans-serif;font-size:11px;color:#4a4d57}
.nav-tag{padding:6px 13px;border-radius:8px;background:rgba(255,255,255,.38);border:1px solid rgba(255,255,255,.82);backdrop-filter:blur(8px);white-space:nowrap}
.nav-right{display:flex;align-items:center;gap:10px;flex-shrink:0}

/* lang toggle */
.lang-row{display:flex;border-radius:8px;overflow:hidden;background:rgba(255,255,255,.38);border:1px solid rgba(255,255,255,.82)}
.lang-btn{padding:6px 12px;border:none;background:transparent;color:rgba(74,77,87,.7);font-size:10.5px;font-weight:600;font-family:'Kanit',sans-serif;cursor:pointer;transition:.18s;letter-spacing:.04em}
.lang-btn.active{background:rgba(22,24,31,.08);color:#16181f}

/* auth badge — renderUserBadge() targets these class names */
.auth-badge{display:flex;align-items:center;gap:8px;border-radius:10px;padding:6px 14px 6px 8px;background:rgba(255,255,255,.42);border:1px solid rgba(255,255,255,.82);backdrop-filter:blur(12px)}
.auth-avatar{width:26px;height:26px;border-radius:50%;border:1.5px solid rgba(200,190,240,.4)}
.auth-avatar-fallback{width:26px;height:26px;border-radius:50%;background:conic-gradient(from 180deg,#c9a6ff,#7cc6ff);display:flex;align-items:center;justify-content:center;font-size:.72rem;font-weight:700;color:#16181f}
.auth-name{font-size:.74rem;font-weight:600;color:#16181f;max-width:120px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.auth-role{font-size:.6rem;font-weight:600;font-family:'Kanit',sans-serif;letter-spacing:.05em;color:#6a6d77}
.auth-logout{background:transparent;border:1px solid rgba(22,24,31,.12);border-radius:7px;color:rgba(22,24,31,.45);font-size:.68rem;padding:3px 9px;cursor:pointer;font-family:inherit;transition:.18s}
.auth-logout:hover{background:rgba(239,68,68,.08);border-color:rgba(239,68,68,.25);color:#b91c1c}
.auth-login-btn{display:inline-flex;align-items:center;gap:6px;border-radius:10px;padding:8px 16px;font-size:.74rem;font-weight:600;font-family:'Kanit',sans-serif;letter-spacing:.04em;text-decoration:none;cursor:pointer;background:#16181f;color:#fff;border:none}

/* ═══════════════════════════════════════════════
   HERO — glass text panel + orb display
═══════════════════════════════════════════════ */
.hero{
  position:relative;z-index:10;
  max-width:1280px;margin:0 auto;
  padding:60px 52px 52px;
  display:grid;grid-template-columns:1.1fr .9fr;gap:28px;align-items:center;
}

/* left glass panel */
.hero-glass{
  background:rgba(255,255,255,.42);border:1px solid rgba(255,255,255,.8);
  backdrop-filter:blur(30px);-webkit-backdrop-filter:blur(30px);
  border-radius:26px;padding:44px 44px 48px;
  box-shadow:0 14px 48px -10px rgba(0,0,0,.22);
  animation:auRise .8s both;
}
.hero-pill{
  display:inline-flex;align-items:center;gap:8px;
  font-family:'Kanit',sans-serif;font-size:10.5px;letter-spacing:.16em;color:#4a4d57;
  background:rgba(255,255,255,.48);border:1px solid rgba(255,255,255,.88);
  padding:7px 15px;border-radius:999px;backdrop-filter:blur(8px);margin-bottom:22px;
}
.hero h1{
  font-weight:600;font-size:clamp(2rem,3.8vw,3.1rem);
  line-height:1.06;letter-spacing:-.02em;margin-bottom:18px;
  animation:auRise .8s .08s both;
}
/* shimmer gradient text — same as AURA */
.grad-text{
  font-family:'Kanit',sans-serif;
  background:linear-gradient(100deg,#5a7bff,#a855f7 35%,#ec4899 60%,#2fb8a0);
  background-size:220% auto;
  -webkit-background-clip:text;background-clip:text;color:transparent;
  animation:auShimmer 6s linear infinite;
}
.hero-desc{font-size:14px;line-height:1.88;font-weight:300;color:#3f424b;max-width:400px;margin-bottom:30px;animation:auRise .8s .16s both}
.hero-btns{display:flex;align-items:center;gap:12px;animation:auRise .8s .24s both}
.btn-dark{background:#16181f;color:#fff;border-radius:11px;padding:13px 26px;font-size:13px;font-weight:500;cursor:pointer;border:none;font-family:inherit;letter-spacing:.01em;transition:background .18s;text-decoration:none;display:inline-block}
.btn-dark:hover{background:#2a2d38}
.btn-glass{background:rgba(255,255,255,.52);border:1px solid rgba(255,255,255,.9);backdrop-filter:blur(10px);border-radius:11px;padding:13px 22px;font-family:'Kanit',sans-serif;font-size:12px;color:#16181f;cursor:pointer;transition:background .18s;letter-spacing:.03em}
.btn-glass:hover{background:rgba(255,255,255,.78)}

/* right — donut chart */
@keyframes auFadeIn{from{opacity:0}to{opacity:1}}
.hero-orb-wrap{position:relative;height:420px;display:flex;align-items:center;justify-content:center}
.donut-wrap{
  position:relative;display:inline-flex;
  align-items:center;justify-content:center;
  animation:auFloat 8s ease-in-out infinite;
  filter:drop-shadow(0 22px 56px rgba(120,90,220,.28));
}
.donut-center{
  position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);
  text-align:center;pointer-events:none;
}
.donut-total{font-family:'Kanit',sans-serif;font-size:30px;font-weight:700;color:#16181f;line-height:1}
.donut-label{font-family:'Kanit',sans-serif;font-size:9px;color:#6a6d77;letter-spacing:.12em;margin-top:3px}
/* legend card color bar */
.legend-bar{width:4px;height:36px;border-radius:4px;flex-shrink:0}
/* floating stat cards */
.stat-float{
  position:absolute;
  background:rgba(255,255,255,.36);border:1px solid rgba(255,255,255,.78);
  backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);
  border-radius:14px;padding:12px 18px;
  box-shadow:0 8px 28px -8px rgba(100,80,200,.15);
}
.stat-float .sf-label{font-family:'Kanit',sans-serif;font-size:10px;color:#6a6d77;letter-spacing:.08em}
.stat-float .sf-val{font-family:'Kanit',sans-serif;font-size:22px;font-weight:600;color:#16181f;line-height:1.2}

/* ═══════════════════════════════════════════════
   CARDS SECTION
═══════════════════════════════════════════════ */
.cards-section{
  position:relative;z-index:10;
  max-width:1280px;margin:0 auto;
  padding:0 52px 72px;
}
.section-label{
  font-family:'Kanit',sans-serif;font-size:10.5px;
  letter-spacing:.16em;color:#6a6d77;margin-bottom:16px;
  animation:auRise .7s .3s both;
}
.cards-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}

/* AURA glass card */
.acard{
  border-radius:18px;padding:32px 28px 28px;
  background:rgba(255,255,255,.4);border:1px solid rgba(255,255,255,.82);
  backdrop-filter:blur(24px);-webkit-backdrop-filter:blur(24px);
  box-shadow:0 10px 34px -12px rgba(0,0,0,.22);
  text-decoration:none;display:flex;flex-direction:column;gap:16px;
  transition:transform .22s cubic-bezier(.34,1.56,.64,1),box-shadow .22s;
  color:inherit;cursor:pointer;position:relative;overflow:hidden;
  animation:auRise .7s both;
}
.acard:hover{transform:translateY(-5px);box-shadow:0 20px 55px -10px rgba(0,0,0,.28)}
#cpe-card  {animation-delay:.1s}
#proj-card {animation-delay:.18s}
#admin-card{animation-delay:.26s}

/* icon float */
.acard-icon{font-size:2.5rem;filter:drop-shadow(0 3px 10px rgba(100,80,200,.22));animation:auFloat var(--fd,5s) ease-in-out var(--fdelay,0s) infinite;display:inline-block}

/* big number */
.acard-num{font-family:'Kanit',sans-serif;font-size:34px;font-weight:700;color:#16181f;line-height:1;margin-top:12px}
.acard-unit{font-size:13px;font-weight:500;color:#6a6d77}
.acard-title{font-size:14px;font-weight:500;color:#16181f;margin-top:4px;line-height:1.3}

/* pills */
.acard-pills{display:flex;gap:6px;flex-wrap:wrap}
.acard-pill{display:inline-flex;align-items:center;gap:5px;font-family:'Kanit',sans-serif;font-size:10.5px;padding:5px 12px;border-radius:8px;border:1px solid rgba(22,24,31,.1);background:rgba(22,24,31,.04);color:#4a4d57;letter-spacing:.03em}

.acard-desc{font-size:12.5px;line-height:1.7;color:#5b5e67;font-weight:300}

/* arrow CTA */
.acard-arrow{display:inline-flex;align-items:center;gap:6px;font-family:'Kanit',sans-serif;font-size:11.5px;font-weight:500;letter-spacing:.04em;color:#16181f;margin-top:auto;transition:gap .18s}
.acard:hover .acard-arrow{gap:10px}

/* visibility */
#cpe-card,#proj-card{display:none}
#admin-card{display:flex;flex-direction:column}

/* ═══════════════════════════════════════════════
   FOOTER
═══════════════════════════════════════════════ */
footer{
  text-align:center;padding:24px;
  color:rgba(74,77,87,.35);font-size:.67rem;
  font-family:'Kanit',sans-serif;letter-spacing:.07em;
  position:relative;z-index:10;
  border-top:1px solid rgba(0,0,0,.06);
}

/* ═══════════════════════════════════════════════
   RESPONSIVE
═══════════════════════════════════════════════ */
@media(max-width:960px){
  nav{padding:16px 24px}
  .nav-mid{display:none}
  .hero{grid-template-columns:1fr;padding:36px 24px 32px;gap:20px}
  .hero-orb-wrap{height:280px}
  .hero-orb{width:200px;height:200px}
  .hero-orb-inner{width:110px;height:110px}
  .hero-orb-inner img{width:64px;height:64px}
  .stat-float{display:none}
  .cards-section{padding:0 24px 60px}
  .cards-grid{grid-template-columns:1fr;gap:12px}
}
@media(max-width:600px){
  .hero h1{font-size:1.85rem}
  .hero-glass{padding:30px 28px 34px}
  .brand-name{font-size:15px}
}
</style>
<script src="auth.js"></script>
<script>if(!getUser()){location.replace('login.html');}</script>
</head>
<body>

<!-- ── reactive orbs ── -->
<div class="au-orb au-orb1" aria-hidden="true"></div>
<div class="au-orb au-orb2" aria-hidden="true"></div>
<div class="au-orb au-orb3" aria-hidden="true"></div>
<div id="au-cursor"         aria-hidden="true"></div>

<!-- ══════════════════════════════════════════
     NAV
══════════════════════════════════════════ -->
<nav>
  <div class="nav-brand">
    <div class="au-dot"></div>
    <span class="brand-name" data-th="คณะเภสัชศาสตร์" data-en="PHARMACY BUU">คณะเภสัชศาสตร์</span>
  </div>

  <div class="nav-mid">
    <span class="nav-tag" data-th="💊 CPE" data-en="💊 CPE">💊 CPE</span>
    <span class="nav-tag" data-th="🚀 โครงการ" data-en="🚀 PROJECTS">🚀 โครงการ</span>
    <span class="nav-tag" data-th="📋 แบบฟอร์ม" data-en="📋 FORMS">📋 แบบฟอร์ม</span>
  </div>

  <div class="nav-right">
    <div id="auth-bar"></div>
    <div class="lang-row">
      <button class="lang-btn active" data-lang="th" onclick="setLangIdx('th')">TH</button>
      <button class="lang-btn"        data-lang="en" onclick="setLangIdx('en')">EN</button>
    </div>
  </div>
</nav>

<!-- ══════════════════════════════════════════
     HERO
══════════════════════════════════════════ -->
<div class="hero">

  <!-- left: glass text panel -->
  <div class="hero-glass">
    <div class="hero-pill">[ ระบบจัดการเอกสาร ] DOCUMENT MANAGEMENT SYSTEM</div>
    <h1>โครงการบริการวิชาการ<br><span class="grad-text">&amp; CPE</span></h1>
    <p class="hero-desc">ศูนย์กลางเอกสาร CPE และโครงการบริการวิชาการ คณะเภสัชศาสตร์ มหาวิทยาลัยบูรพา — จัดการและเข้าถึงได้ทุกที่ทุกเวลา</p>
    <div class="hero-btns">
      <a href="academic-form-system.html" class="btn-dark" data-th="จัดการแบบฟอร์ม →" data-en="MANAGE FORMS →">จัดการแบบฟอร์ม →</a>
      <button class="btn-glass" onclick="document.querySelector('.cards-section').scrollIntoView({behavior:'smooth'})" data-th="ดูเอกสาร" data-en="VIEW DOCS">/docs</button>
    </div>
  </div>

  <!-- right: SVG donut chart — CPE vs โครงการวิชาการ -->
  <div class="hero-orb-wrap">

    <!-- ambient bg glow (reactive to --hue) -->
    <div style="position:absolute;width:330px;height:330px;border-radius:50%;background:conic-gradient(from 0deg,#86c5ff55,#c5a0ff55,#ff95d055,#95f5d055,#86c5ff55);filter:blur(52px);opacity:.55;animation:auSpin 28s linear infinite;pointer-events:none"></div>

    <!-- SVG donut — r=115, C≈722.6 -->
    <!-- CPE  (17.9%): 125.5 visible, offset=-2  → arc from 2..127.5 -->
    <!-- Proj (82.1%): 589.0 visible, offset=-131.5 → arc from 131.5..720.5 -->
    <div class="donut-wrap">
      <svg viewBox="0 0 300 300" width="272" height="272" aria-hidden="true">
        <defs>
          <!-- CPE: blue → purple -->
          <linearGradient id="g-cpe" gradientUnits="userSpaceOnUse" x1="60" y1="30" x2="240" y2="270">
            <stop offset="0%"   stop-color="#7cc6ff"/>
            <stop offset="100%" stop-color="#b9a4ff"/>
          </linearGradient>
          <!-- โครงการ: warm gold → rose -->
          <linearGradient id="g-proj" gradientUnits="userSpaceOnUse" x1="240" y1="30" x2="60" y2="270">
            <stop offset="0%"   stop-color="#ffd090"/>
            <stop offset="100%" stop-color="#ff9ecf"/>
          </linearGradient>
          <!-- subtle inner shadow -->
          <filter id="inner-glow">
            <feGaussianBlur stdDeviation="4" result="blur"/>
            <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
          </filter>
        </defs>

        <!-- track ring -->
        <circle cx="150" cy="150" r="115" fill="none"
          stroke="rgba(255,255,255,.2)" stroke-width="30"/>

        <g transform="rotate(-90 150 150)">
          <!-- โครงการวิชาการ arc (82.1%, warm gold→rose) -->
          <circle cx="150" cy="150" r="115" fill="none"
            stroke="url(#g-proj)" stroke-width="30"
            stroke-dasharray="589.0 133.6"
            stroke-dashoffset="-131.5"
            stroke-linecap="butt"
            style="animation:auFadeIn 1s .5s both"/>

          <!-- CPE arc (17.9%, blue→purple) -->
          <circle cx="150" cy="150" r="115" fill="none"
            stroke="url(#g-cpe)" stroke-width="30"
            stroke-dasharray="125.5 597.1"
            stroke-dashoffset="-2"
            stroke-linecap="butt"
            style="animation:auFadeIn 1s .2s both"/>
        </g>

        <!-- glass inner circle -->
        <circle cx="150" cy="150" r="80"
          fill="rgba(255,255,255,.46)"
          stroke="rgba(255,255,255,.88)"
          stroke-width="1.5"/>

        <!-- percentage labels on arcs -->
        <!-- วิชาการ label at ~240° from top (82.1% midpoint ≈ 131.5 + 294.5 = 426 → 426/722.6*360 = 212°) -->
        <text x="150" y="150" text-anchor="middle" dominant-baseline="middle"
          transform="rotate(122 150 150) translate(0 -115)"
          font-family="Kanit,sans-serif" font-size="10.5" font-weight="700" fill="rgba(255,255,255,.92)"
          style="animation:auFadeIn 1s .8s both">82.1%</text>
        <!-- CPE label at ~64°/2=32° from top (CPE midpoint ≈ 64.5° → 32° + (-90°) = -58° from SVG zero, so in rotated space: 64.5°/2=32°) -->
        <text x="150" y="150" text-anchor="middle" dominant-baseline="middle"
          transform="rotate(-58 150 150) translate(0 -115)"
          font-family="Kanit,sans-serif" font-size="10.5" font-weight="700" fill="rgba(255,255,255,.92)"
          style="animation:auFadeIn 1s .8s both">17.9%</text>
      </svg>

      <!-- center: total count -->
      <div class="donut-center">
        <div class="donut-total">2,588</div>
        <div class="donut-label">TOTAL FILES</div>
      </div>
    </div>

    <!-- CPE legend card (top-left, blue) -->
    <div class="stat-float" style="top:12px;left:-14px;animation:auFloat 7s ease-in-out .4s infinite">
      <div style="display:flex;align-items:center;gap:10px">
        <div class="legend-bar" style="background:linear-gradient(180deg,#7cc6ff,#b9a4ff)"></div>
        <div>
          <div class="sf-label">💊 CPE</div>
          <div class="sf-val">464</div>
          <div style="font-family:'Kanit',sans-serif;font-size:9px;color:rgba(106,109,119,.85);margin-top:2px;letter-spacing:.04em">17.9% · 281 โฟลเดอร์</div>
        </div>
      </div>
    </div>

    <!-- โครงการวิชาการ legend card (bottom-right, rose) -->
    <div class="stat-float" style="bottom:14px;right:-10px;animation:auFloatB 6s ease-in-out infinite">
      <div style="display:flex;align-items:center;gap:10px">
        <div class="legend-bar" style="background:linear-gradient(180deg,#ffd090,#ff9ecf)"></div>
        <div>
          <div class="sf-label">🚀 โครงการวิชาการ</div>
          <div class="sf-val">2,124</div>
          <div style="font-family:'Kanit',sans-serif;font-size:9px;color:rgba(106,109,119,.85);margin-top:2px;letter-spacing:.04em">82.1% · 344 โฟลเดอร์</div>
        </div>
      </div>
    </div>

  </div>

</div>

<!-- ══════════════════════════════════════════
     CARDS
══════════════════════════════════════════ -->
<div class="cards-section">
  <div class="section-label">[ เลือกหมวดหมู่ ] CHOOSE A SECTION</div>
  <div class="cards-grid">

    <!-- CPE -->
    <a class="acard" id="cpe-card" href="cpe.html" style="--fd:5s;--fdelay:.1s">
      <div class="acard-icon">💊</div>
      <div>
        <div class="acard-num">464 <span class="acard-unit">files</span></div>
        <div class="acard-title" data-th="CPE · การศึกษาต่อเนื่องทางเภสัชกรรม" data-en="CPE · Continuing Pharmacy Education">CPE · การศึกษาต่อเนื่องทางเภสัชกรรม</div>
      </div>
      <div class="acard-pills">
        <span class="acard-pill">✦ <span data-count="464">464</span>&thinsp;<span data-th="ไฟล์" data-en="FILES">ไฟล์</span></span>
        <span class="acard-pill">◈ <span data-count="281">281</span>&thinsp;<span data-th="โฟลเดอร์" data-en="FOLDERS">โฟลเดอร์</span></span>
      </div>
      <p class="acard-desc" data-th="เอกสาร CPE สำหรับเภสัชกร เข้าถึงไฟล์และโฟลเดอร์ได้โดยตรง" data-en="CPE documents for pharmacists, direct access to files and folders">เอกสาร CPE สำหรับเภสัชกร เข้าถึงไฟล์และโฟลเดอร์ได้โดยตรง</p>
      <span class="acard-arrow"><span data-th="เปิดดูเอกสาร" data-en="BROWSE DOCS">เปิดดูเอกสาร</span> →</span>
    </a>

    <!-- PROJECTS -->
    <a class="acard" id="proj-card" href="projects.html" style="--fd:4.6s;--fdelay:.45s">
      <div class="acard-icon">🚀</div>
      <div>
        <div class="acard-num">2,124 <span class="acard-unit">files</span></div>
        <div class="acard-title" data-th="โครงการบริการวิชาการ" data-en="Academic Service Projects">โครงการบริการวิชาการ</div>
      </div>
      <div class="acard-pills">
        <span class="acard-pill">✦ <span data-count="2124">2,124</span>&thinsp;<span data-th="ไฟล์" data-en="FILES">ไฟล์</span></span>
        <span class="acard-pill">◈ <span data-count="344">344</span>&thinsp;<span data-th="โฟลเดอร์" data-en="FOLDERS">โฟลเดอร์</span></span>
      </div>
      <p class="acard-desc" data-th="เอกสารโครงการบริการวิชาการ ค้นหาและเข้าถึงง่าย" data-en="Academic service project documents, easy to find and access">เอกสารโครงการบริการวิชาการ ค้นหาและเข้าถึงง่าย</p>
      <span class="acard-arrow"><span data-th="เปิดดูเอกสาร" data-en="BROWSE DOCS">เปิดดูเอกสาร</span> →</span>
    </a>

    <!-- ADMIN FORM -->
    <a class="acard" id="admin-card" href="academic-form-system.html" style="--fd:5.3s;--fdelay:.8s">
      <div class="acard-icon">📋</div>
      <div>
        <div class="acard-num" style="font-size:26px;line-height:1.15">ฟอร์ม<span class="acard-unit" style="font-size:15px">วิชาการ</span></div>
        <div class="acard-title">Academic Form System</div>
      </div>
      <div class="acard-pills">
        <span class="acard-pill">📋 กรอกแบบฟอร์ม</span>
        <span class="acard-pill">📄 คำสั่งแต่งตั้ง</span>
      </div>
      <p class="acard-desc">สร้างและจัดการแบบฟอร์มวิชาการ ออกคำสั่งแต่งตั้ง Export PDF และ Word ได้ทันที</p>
      <span class="acard-arrow">จัดการแบบฟอร์ม →</span>
    </a>

  </div>
</div>

<footer data-th="DOCUMENT SYSTEM · คณะเภสัชศาสตร์ มหาวิทยาลัยบูรพา" data-en="DOCUMENT SYSTEM · FACULTY OF PHARMACY, BURAPHA UNIVERSITY">
  DOCUMENT SYSTEM · คณะเภสัชศาสตร์ มหาวิทยาลัยบูรพา
</footer>

<script>
/* ── Language ── */
function setLangIdx(l){
  document.querySelectorAll('.lang-btn').forEach(b=>b.classList.toggle('active',b.dataset.lang===l));
  document.querySelectorAll('[data-th]').forEach(el=>{const v=el.getAttribute('data-'+l);if(v)el.innerHTML=v;});
}

/* ── AURA reactive system (ported 1:1 from standalone componentDidMount) ── */
(function(){
  let tx=.5,ty=.5,cx=.5,cy=.5;
  const cursor=document.getElementById('au-cursor');

  document.addEventListener('mousemove',e=>{
    tx=e.clientX/window.innerWidth;
    ty=e.clientY/window.innerHeight;
    if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px';}
  });

  (function tick(){
    cx+=(tx-cx)*.055; cy+=(ty-cy)*.055;
    document.body.style.setProperty('--mx',cx.toFixed(4));
    document.body.style.setProperty('--my',cy.toFixed(4));
    document.body.style.setProperty('--hue',(cx*220).toFixed(1));
    // reactive background like AURA: oklch shifts with hue
    const hue=170+cx*100;
    document.body.style.background='oklch(94.5% 0.018 '+hue.toFixed(0)+')';
    requestAnimationFrame(tick);
  })();
})();

/* ── DOMContentLoaded ── */
document.addEventListener('DOMContentLoaded',()=>{

  /* animated counters */
  document.querySelectorAll('[data-count]').forEach(el=>{
    const tg=parseInt(el.dataset.count),dur=1400,st=performance.now();
    function u(now){const p=Math.min((now-st)/dur,1),e=1-Math.pow(1-p,4);el.textContent=Math.round(e*tg).toLocaleString();if(p<1)requestAnimationFrame(u);}
    requestAnimationFrame(u);
  });

  /* card ripple */
  document.querySelectorAll('.acard').forEach(card=>{
    card.addEventListener('click',e=>{
      const r=card.getBoundingClientRect(),sz=Math.max(r.width,r.height)*2.2;
      const rpl=document.createElement('span');
      rpl.style.cssText=`position:absolute;pointer-events:none;border-radius:50%;width:${sz}px;height:${sz}px;left:${e.clientX-r.left-sz/2}px;top:${e.clientY-r.top-sz/2}px;background:rgba(90,123,255,.1);transform:scale(0);animation:rpl .58s ease-out forwards;z-index:10`;
      card.appendChild(rpl);setTimeout(()=>rpl.remove(),620);
    });
  });
});

/* ── Auth init ── */
(function(){
  const u=getUser();
  if(u){
    renderUserBadge('auth-bar',u);
    if(isAdmin(u)){
      const cpe=document.getElementById('cpe-card');
      const proj=document.getElementById('proj-card');
      if(cpe) cpe.style.display='flex';
      if(proj)proj.style.display='flex';
    }
  } else {
    document.getElementById('auth-bar').innerHTML=
      '<a class="auth-login-btn" href="login.html">🔑 เข้าสู่ระบบ</a>';
  }
})();
</script>
</body>
</html>
